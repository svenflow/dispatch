{
  "name": "nicklaude-search",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run src/daemon.ts",
    "start": "bun run src/daemon.ts",
    "cli": "bun run src/cli.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build": "bun build src/index.ts --outdir dist --target bun"
  },
  "dependencies": {
    "node-llama-cpp": "^3.14.5",
    "yaml": "^2.3.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
