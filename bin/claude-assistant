#!/bin/bash
# Claude Assistant CLI wrapper
# Runs the Python CLI using uv

ASSISTANT_DIR="$HOME/dispatch"
UV="$(command -v uv || echo "$HOME/.local/bin/uv")"

cd "$ASSISTANT_DIR" && exec "$UV" run python -m assistant.cli "$@"
